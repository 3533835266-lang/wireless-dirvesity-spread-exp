\# 无线通信技术实验报告：分集与扩频通信



\## 1. 实验目的



本实验旨在通过编程实践深入理解无线通信系统中的衰落现象，以及应对信道衰落和外部干扰的关键技术。具体的实验目的包括以下几个层面：

第一，通过构建瑞利平坦衰落信道模型，深刻理解无线信道多径效应造成的深衰落现象，并掌握如何通过信道仿真评估系统的误码率（BER）性能。

第二，通过代码亲自实现选择合并（SC）、最大比合并（MRC）等经典的接收端分集技术，对比不同合并策略的内部原理以及分集阶数对改善系统链路可靠性、降低误码率的影响。同时挑战完成等增益合并（EGC）的选做任务，加深对合并权重的理解。

第三，掌握直接序列扩频（DSSS）的“扩频-信道-解扩”完整流程。学习通过线性反馈移位寄存器（LFSR）生成伪随机（PN）的 m 序列，掌握使用高速码片序列扩展信号频谱带宽的方法，以及计算理论处理增益的方法。

第四，通过人为向信道中加入窄带干扰和高斯白噪声，验证扩频通信体制依靠相关解扩在接收端有效分离出目标信号并分散窄带干扰的优越抗干扰特性，并尝试完成同步偏移搜索选做任务。

第五，熟练使用 NumPy 进行矩阵化的高效运算，掌握并应用自动化测试框架（pytest）和 GitHub Actions 的自动化评分及集成工作流规范。



\## 2. 实验原理



\### 2.1 分集合并

在无线链路中，发送信号在传播过程中会受到多径效应、遮挡和移动环境等影响，导致接收端出现快速的幅度和相位波动。若所有路径的信号发生相消干涉，系统即陷入“深衰落”。

分集的核心思想是：让接收端获得多个统计上独立的信号副本（如空间分集、频率分集等）。只要这些副本不会同时处于深衰落，就能有效恢复原始信号。

\- \*\*选择合并 SC\*\*：在接收端的各分支中，计算每一路瞬时信道的强度 `|h|^2`，选择具有最大信道功率的分支作为唯一的输出分支进行处理，丢弃其他分支的信息。这种方法实现简单，但未充分利用全部能量。

\- \*\*最大比合并 MRC\*\*：各个分支先利用信道系数的共轭 `conj(h)` 进行相位校正，接着按信道幅度进行加权相加，最终除以各分支信道功率的总和 `sum(|h|^2)`。在独立加性高斯白噪声（AWGN）条件下，MRC 可以实现接收端信噪比的最大化，通常其性能严格优于 SC 机制。

\- \*\*等增益合并 EGC\*\*：仅利用信道系数进行相位校正，即各个支路信号乘以 `conj(h) / |h|` ，使所有的分支相干同相后直接等幅相加。由于没有利用信道衰落的幅度信息加权，性能略逊于 MRC。



\### 2.2 扩频通信

直接序列扩频（DSSS）是指将需要发送的窄带信息序列，与一个速率远高于信息速率的伪随机（PN）码片序列进行乘法运算。

\- \*\*m 序列与扩频\*\*：m 序列可以通过一定级数的线性反馈移位寄存器（LFSR）生成，若多项式选择得当，可以得到长度为 `2^m - 1` 的最大周期伪随机序列。发送端扩频时，将数据比特映射成 BPSK 符号，再通过与 PN 码的外积，将原始频谱在频域展宽。

\- \*\*相关解扩与抗干扰\*\*：在接收端，利用完全相同且同步的 PN 序列与接收码片逐段进行内积相关计算。由于 PN 码自相关性极强，期望信号会被重新聚合并恢复出原始能量（处理增益）；而信道中的窄带干扰在解扩操作下，由于与 PN 码不相关，其频谱反而会被展宽，能量被极大摊薄。

\- \*\*处理增益\*\*：系统的处理增益理论值主要由扩频因子 `N` 决定，公式为 `G\_p = 10 \* log10(N)`，单位为 dB。



\## 3. 实验环境



本实验采用了标准的 Python 通信仿真技术栈，主要配置如下：

\- \*\*Python 版本\*\*：Python 3.9 或更高版本（实际本地测试时使用 Python 3.13）。

\- \*\*主要依赖库\*\*：NumPy 用于矩阵向量运算（`np.outer`，`np.sum` 等）；Matplotlib 用于数据结果可视化；pytest 框架用于进行自动化单元测试与评分校验；pylint 用于把控代码编写质量与风格检查。

\- \*\*AI 助手使用情况\*\*：本次实验中，编写核心算法时适度引入了 AI 工具（如 Copilot / 大语言模型），主要应用在辅助检查 NumPy 矩阵化运算操作的维度匹配问题上，同时请 AI 帮忙校验了解扩相关矩阵乘法 `matrix @ pn\_chips` 以及辅助排查测试脚本工作目录定位不准确的路径问题，但在最终提交前，我已经独立对所有的合并、解扩逻辑进行了人工推演和梳理验证。



\## 4. 实验方法与步骤



\### 4.1 Part 1：分集合并

1\. 先利用 `generate\_bits` 随机产生数据源，使用 `bpsk\_modulate` 进行 BPSK 的基带调制。

2\. 配置一个循环，遍历给定的各个 SNR 阈值。通过调用底层 API 产生带有特定 SNR 的具有多分支的独立瑞利平坦衰落信道 `h` 和受污染接收序列 `r`。

3\. 利用数组的高级索引操作，找出每列信道衰减幅度最大的行索引，实现 SC（选择合并）策略的检测恢复并计算误码率。

4\. 调用自行编写的 MRC 算法逻辑，使用 `np.sum(np.conj(channel) \* received, axis=0) / np.sum(np.abs(channel)\*\*2, axis=0)` 精确求出恢复信号后计算误码率。

5\. 将仿真过程封装字典，保存生成的无分集、SC 分集与 MRC 分集在不同信噪比下的误码率结果。



\### 4.2 Part 2：DSSS 扩频通信

1\. 依据右移位反馈规则及 `tap` 抽头约束设计 LFSR m 序列发生器函数，并在输出时做 `0 -> +1`、`1 -> -1` 的双极性电平转换。

2\. 扩频部分利用 `np.outer` 的矩阵外积运算，一行代码完成所有比特的扩频和展宽，并通过 `flatten` 展平输出。

3\. 仿照现实通信链路，加入特定的窄带干扰余弦波和加性白噪声，模拟恶劣的电磁空间环境。

4\. 解扩部分将含有干扰信号的序列重新用扩频因子 N 进行 `reshape`，通过与本地同步生成的参考 PN 序列进行矩阵乘法（相关运算），并根据乘积符号正负判断原基带数据的 0/1 特性。



\## 5. 实验结果



经过完整的实验代码编写和终端运行，成功得到了各个指标的验证反馈。以下为代码生成的仿真结果图：



!\[分集BER曲线](results/diversity\_ber\_curve.png)



!\[分集合并波形](results/diversity\_waveform\_snapshot.png)



!\[DSSS BER曲线](results/dsss\_ber\_curve.png)



!\[DSSS相关输出](results/dsss\_correlation\_snapshot.png)



\## 6. 结果分析与讨论



针对实验提出的核心问题，综合理论与实验仿真的现象做出如下详细回答：



\*\*1. 为什么瑞利衰落会造成深衰落？\*\*

瑞利衰落描述的是无线通信中不存在直射路径、全由大量的随机散射和反射多径信号叠加构成的信道。由于不同路径的信号相位是完全随机的，当这些具有相反相位的多径分量到达接收天线发生相消干涉时，合成总包络就会骤降到极小的数值，这就是通信中非常棘手的深衰落。



\*\*2. SC 和 MRC 的合并思想有什么不同？\*\*

SC（选择合并）是“优中选优”的思想，它监控每个支路的信噪比（由信道幅度平方衡量），每过一个符号周期就切换到信道条件最好的一路，这浪费了其他支路上有价值的信号能量；而 MRC（最大比合并）是“通力协作”的思想，它通过共轭相位调整将所有支路信号同相叠加，并给信噪比高的支路赋予更高的权重，保证了各支路信息的最大化利用。



\*\*3. 为什么 MRC 通常优于 SC？\*\*

由上一条可知，SC 在任何时刻都仅仅提取了单支路信号；而在 MRC 算法中，它能够合并所有分支上的有用信号能量。从理论公式也可证明，在独立高斯白噪声环境下，MRC 合并输出的信噪比等于参与合并的所有分支瞬时信噪比之和，这是各合并策略中理论能达到的性能上限，因此它能在相同误码率下提供最大的系统增益。



\*\*4. DSSS 的处理增益如何由扩频因子决定？\*\*

扩频因子 N 是指一个数据比特占用的 PN 码片个数。由于目标信号在解扩时同相累加，能量放大 N 倍，而随机噪声（和干扰）累加后能量近似不变或者仅仅是方差的累积，这相当于在解扩环节后额外获得了 N 倍的信噪比提升，将其转化为 dB 值就是经典的 `10 \* log10(N)` 公式。



\*\*5. 窄带干扰经过解扩后为什么会被摊薄？\*\*

因为接收端的解扩操作实质上等同于用一个极为宽带的伪随机序列（PN 码）再去对接收信号进行一次频率“扩频”。对于同步好的原始宽带信号来说，二次扩频将其挤压回了信息窄带；而对于信道中新混入的窄带干扰，经过这个操作则如同进行了初始扩频一样，其能量频谱被扩展成了类似于白噪声的很宽的频带。在接下来的窄带低通滤波/积分判决过程中，只有极少部分落入基带带宽内的干扰能量会留存，绝大多数干扰能量已被滤除。



\*\*6. 本实验中 BER 曲线是否符合理论预期？\*\*

完全符合理论预期。从 Part 1 的曲线图中明显看到，单分支在信噪比逐渐增加时，曲线下降非常缓慢（几乎呈反比例线性衰减）；而加入了 SC 和 MRC 分集后，随着 SNR 的增大，误码率呈指数级骤降，且相同 SNR 下 MRC 的 BER 明显低于 SC。在 Part 2 中，由于我们主动加入了强劲的窄带干扰，未扩频信号的 BER 无论如何提高信噪比都卡在一个很高的错误下限，而利用 N=31 扩频后的链路，凭借超过 14dB 的处理增益彻底将干扰压制，BER 恢复了平滑的下降瀑布曲线。



\## 7. 实验心得



通过本次综合性的代码实验，我对于《无线通信技术》教材中关于衰落对抗和抗干扰体制这两块硬核理论有了具象化的理解。特别是亲手利用 NumPy 的矩阵切片运算实现了选择合并以及扩频序列对消后，那些原本在纸面上显得复杂的累加公式和对角线权值变得豁然开朗。



在工程实践层面，这套实验框架加深了我对现代化软件测试标准的理解。评分脚本 `calculate\_grade.py` 对各模块严格的 pytest 校验和代码规范检查督促我在编码时仔细思考异常捕获与边界约束（比如 `np.isfinite` 检验，矩阵维度强制对其的预检查）。我也成功在自动评分系统上体会到通过调试解决依赖库问题、工作目录路径问题最终拿到满分的成就感。在随后的选做任务中，自己动手推演实现了等增益合并和偏移搜索，加深了对于数字通信信号同步难度的认知。总体而言，这次项目是一次极佳的通信基础理论与现代 Python 工程相融合的练兵。



\## 8. 参考资料



\- 课程课件：第8章 分集

\- 课程课件：第9章 扩展频谱通信

\- 《无线通信基础》（Tse \& Viswanath）相关章节

\- NumPy 官方文档 - numpy.outer、numpy.argmax

